@page "/client/profile"
@using Bookify.Client.Models.Profile
@attribute [Authorize(Policy = "ClientOnly")]


<PageTitle>My Profile — Bookify</PageTitle>

<AuthorizeView Policy="ClientOnly" Context="clientContext">
    <Authorized>
        <!-- Background decoration -->
        <div class="fixed inset-0 z-[-1] pointer-events-none bg-gray-50/50">
            <div class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] rounded-full bg-green-600/5 blur-[120px]"></div>
            <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] rounded-full bg-emerald-600/5 blur-[120px]"></div>
        </div>

        <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            
            <!-- Header -->
            <div class="mb-10 flex flex-col md:flex-row md:items-end justify-between gap-4">
                <div>
                    <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-green-100 text-green-700 text-xs font-bold uppercase tracking-wider mb-3">
                        <i class="fas fa-leaf"></i> Client Portal
                    </div>
                    <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight">My Profile</h1>
                    <p class="text-gray-500 mt-2 text-lg">Manage your personal information and account security.</p>
                </div>
            </div>

            @if (_loading)
            {
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    <div class="h-80 bg-white rounded-[2rem] border border-gray-100 shadow-sm animate-pulse"></div>
                    <div class="lg:col-span-2 h-[50rem] bg-white rounded-[2rem] border border-gray-100 shadow-sm animate-pulse"></div>
                </div>
            }
            else if (_profile is null)
            {
                <div class="flex flex-col items-center justify-center py-20 bg-white/50 backdrop-blur-xl rounded-[2.5rem] border border-gray-100 shadow-xl">
                    <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mb-6">
                        <i class="fas fa-user-slash text-4xl text-gray-400"></i>
                    </div>
                    <h2 class="text-2xl font-bold text-gray-900 mb-2">Profile Unavailable</h2>
                    <p class="text-gray-500">Could not load your profile details. Please try refreshing the page.</p>
                </div>
            }
            else
            {
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    
                    <!-- Avatar Card -->
                    <div class="bg-white/80 backdrop-blur-xl rounded-[2rem] border border-gray-100 shadow-xl shadow-gray-200/40 p-8 flex flex-col items-center text-center gap-5 relative overflow-hidden group">
                        <div class="absolute top-0 left-0 w-full h-32 bg-gradient-to-br from-green-600/10 to-emerald-600/10 -z-10 group-hover:h-full transition-all duration-500"></div>
                        
                        <div class="relative">
                            <div class="absolute inset-0 bg-green-600 rounded-full blur-xl opacity-20 group-hover:opacity-40 transition-opacity duration-300"></div>
                            <div class="w-28 h-28 rounded-full bg-gradient-to-br from-green-500 to-emerald-600 flex items-center justify-center shadow-2xl shadow-green-500/30 ring-4 ring-white relative z-10 transform group-hover:scale-105 transition-transform duration-300 overflow-hidden">
                                @if (string.IsNullOrEmpty(_profile.ImagePath))
                                {
                                    <span class="text-white text-5xl font-black shadow-sm">@GetInitial()</span>
                                }
                                else
                                {
                                    <img src="@_profile.ImagePath" class="w-full h-full object-cover" alt="Profile" />
                                }
                                
                                <!-- Upload Overlay -->
                                <label for="profileImg" class="absolute inset-0 bg-black/40 flex flex-col items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity cursor-pointer z-20">
                                    <i class="fas fa-camera text-white text-xl mb-1"></i>
                                    <span class="text-white text-[10px] font-bold uppercase tracking-tighter">Change</span>
                                    <InputFile id="profileImg" OnChange="HandleFileSelected" class="hidden" accept=".jpg,.jpeg,.png" />
                                </label>
                            </div>
                            
                            @if (_uploading)
                            {
                                <div class="absolute -bottom-2 -right-2 w-8 h-8 bg-white rounded-full shadow-lg flex items-center justify-center z-30">
                                    <i class="fas fa-circle-notch animate-spin text-green-600"></i>
                                </div>
                            }
                        </div>

                        <div class="mt-2">
                            <h3 class="text-2xl font-black text-gray-900 tracking-tight">@_profile.FullName</h3>
                            <p class="text-gray-500 font-medium flex items-center justify-center gap-2 mt-1">
                                <i class="fas fa-envelope text-gray-400"></i> @_profile.Email
                            </p>
                        </div>

                        <div class="w-full h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent my-2"></div>

                        <div class="w-full flex flex-col gap-3">
                            <div class="px-5 py-2.5 rounded-2xl bg-gradient-to-r from-green-50 to-emerald-50 border border-green-100 text-green-700 font-bold text-sm shadow-sm flex items-center justify-center gap-2">
                                <i class="fas fa-user text-green-500"></i> Client Account
                            </div>
                            
                            @if (_profile.DateOfBirth.HasValue)
                            {
                                <div class="px-5 py-2.5 rounded-2xl bg-gray-50 border border-gray-100 text-gray-700 font-semibold text-sm flex items-center justify-between">
                                    <span class="text-gray-400 text-xs uppercase tracking-wider font-bold">Birthday</span>
                                    <span>@_profile.DateOfBirth.Value.ToString("MMM dd, yyyy")</span>
                                </div>
                            }

                            @if (_profile.Gender.HasValue)
                            {
                                <div class="px-5 py-2.5 rounded-2xl bg-gray-50 border border-gray-100 text-gray-700 font-semibold text-sm flex items-center justify-between">
                                    <span class="text-gray-400 text-xs uppercase tracking-wider font-bold">Gender</span>
                                    <span>@GetGenderLabel(_profile.Gender.Value)</span>
                                </div>
                            }
                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="lg:col-span-2 flex flex-col gap-8">

                        <!-- Personal Info Form -->
                        <div class="bg-white/80 backdrop-blur-xl rounded-[2rem] border border-gray-100 shadow-xl shadow-gray-200/40 p-8 md:p-10 relative overflow-hidden">
                            <div class="absolute top-0 right-0 w-64 h-64 bg-green-600/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/3"></div>
                            
                            <div class="flex items-center gap-4 mb-8 relative z-10">
                                <div class="w-12 h-12 rounded-2xl bg-green-100 flex items-center justify-center text-green-600 text-xl shadow-inner">
                                    <i class="fas fa-user-edit"></i>
                                </div>
                                <div>
                                    <h2 class="text-xl font-extrabold text-gray-900">Personal Details</h2>
                                    <p class="text-sm text-gray-500 font-medium">Update your name, phone, and preferences.</p>
                                </div>
                            </div>

                            <div class="space-y-6 relative z-10">
                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Full Name</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-green-500 transition-colors">
                                                <i class="fas fa-id-card"></i>
                                            </div>
                                            <input type="text" @bind="_edit.FullName"
                                                   class="w-full pl-11 pr-4 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-green-500 focus:ring-4 focus:ring-green-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" placeholder="Enter your full name" />
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Phone Number</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-green-500 transition-colors">
                                                <i class="fas fa-phone-alt"></i>
                                            </div>
                                            <input type="tel" @bind="_edit.Phone"
                                                   class="w-full pl-11 pr-4 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-green-500 focus:ring-4 focus:ring-green-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" placeholder="Phone number" />
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Gender</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-green-500 transition-colors z-10">
                                                <i class="fas fa-venus-mars"></i>
                                            </div>
                                            <select @bind="_genderValue"
                                                    class="w-full pl-11 pr-10 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-green-500 focus:ring-4 focus:ring-green-500/10 outline-none transition-all text-gray-900 font-bold appearance-none cursor-pointer">
                                                <option value="0">Female</option>
                                                <option value="1">Male</option>
                                                <option value="2">Prefer not to say</option>
                                            </select>
                                            <div class="absolute right-4 text-gray-400 pointer-events-none">
                                                <i class="fas fa-chevron-down text-sm"></i>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Date of Birth</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-green-500 transition-colors">
                                                <i class="fas fa-calendar-alt"></i>
                                            </div>
                                            <input type="date" @bind="_edit.DateOfBirth"
                                                   class="w-full pl-11 pr-4 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-green-500 focus:ring-4 focus:ring-green-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" />
                                        </div>
                                    </div>
                                </div>

                                <div class="relative group">
                                    <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Email Address</label>
                                    <div class="relative flex items-center">
                                        <div class="absolute left-4 text-gray-400">
                                            <i class="fas fa-envelope"></i>
                                        </div>
                                        <input type="email" value="@_profile.Email" readonly
                                               class="w-full pl-11 pr-4 py-3.5 bg-gray-100 rounded-2xl border-2 border-transparent text-gray-500 font-bold cursor-not-allowed opacity-70" />
                                        <div class="absolute right-4 text-xs font-bold text-gray-400 uppercase bg-gray-200 px-2 py-1 rounded-md">Read Only</div>
                                    </div>
                                </div>

                                <div class="pt-4 flex justify-end">
                                    <button @onclick="SaveProfileAsync" disabled="@_saving"
                                            class="w-full sm:w-auto px-8 py-4 bg-gradient-to-r from-green-600 to-emerald-600 text-white font-extrabold rounded-2xl hover:from-green-700 hover:to-emerald-700 transition-all shadow-xl shadow-green-500/30 hover:-translate-y-1 hover:shadow-green-500/50 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:translate-y-0 relative overflow-hidden group/btn">
                                        <div class="absolute inset-0 w-full h-full bg-gradient-to-r from-transparent via-white/20 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-1000 ease-in-out"></div>
                                        <div class="relative flex items-center justify-center gap-2">
                                            @if (_saving)
                                            {
                                                <i class="fas fa-circle-notch animate-spin"></i>
                                                <span>Saving Changes...</span>
                                            }
                                            else
                                            {
                                                <i class="fas fa-check-circle"></i>
                                                <span>Save Profile</span>
                                            }
                                        </div>
                                    </button>
                                </div>
                            </div>
                        </div>

                        <!-- Change Password Form -->
                        <div class="bg-white/80 backdrop-blur-xl rounded-[2rem] border border-gray-100 shadow-xl shadow-gray-200/40 p-8 md:p-10 relative overflow-hidden">
                            <div class="absolute top-0 left-0 w-64 h-64 bg-amber-500/5 rounded-full blur-3xl -translate-y-1/2 -translate-x-1/3"></div>

                            <div class="flex items-center gap-4 mb-8 relative z-10">
                                <div class="w-12 h-12 rounded-2xl bg-amber-100 flex items-center justify-center text-amber-600 text-xl shadow-inner">
                                    <i class="fas fa-key"></i>
                                </div>
                                <div>
                                    <h2 class="text-xl font-extrabold text-gray-900">Security</h2>
                                    <p class="text-sm text-gray-500 font-medium">Update your password regularly to keep your account safe.</p>
                                </div>
                            </div>

                            <div class="space-y-6 relative z-10">
                                <div class="relative group">
                                    <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Current Password</label>
                                    <div class="relative flex items-center">
                                        <div class="absolute left-4 text-gray-400 group-focus-within:text-amber-500 transition-colors">
                                            <i class="fas fa-lock"></i>
                                        </div>
                                        <input type=@(_showCurrent ? "text" : "password") @bind="_pwd.CurrentPassword"
                                               class="w-full pl-11 pr-12 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-amber-500 focus:ring-4 focus:ring-amber-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" placeholder="••••••••" />
                                        <button type="button" @onclick="() => _showCurrent = !_showCurrent" class="absolute right-4 text-gray-400 hover:text-gray-700 transition-colors w-6 h-6 flex items-center justify-center">
                                            <i class="fas @(_showCurrent ? "fa-eye-slash" : "fa-eye")"></i>
                                        </button>
                                    </div>
                                </div>

                                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">New Password</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-amber-500 transition-colors">
                                                <i class="fas fa-shield-alt"></i>
                                            </div>
                                            <input type=@(_showNew ? "text" : "password") @bind="_pwd.NewPassword"
                                                   class="w-full pl-11 pr-12 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-amber-500 focus:ring-4 focus:ring-amber-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" placeholder="••••••••" />
                                            <button type="button" @onclick="() => _showNew = !_showNew" class="absolute right-4 text-gray-400 hover:text-gray-700 transition-colors w-6 h-6 flex items-center justify-center">
                                                <i class="fas @(_showNew ? "fa-eye-slash" : "fa-eye")"></i>
                                            </button>
                                        </div>
                                    </div>

                                    <div class="relative group">
                                        <label class="block text-xs font-black text-gray-400 uppercase tracking-widest mb-2 ml-1">Confirm Password</label>
                                        <div class="relative flex items-center">
                                            <div class="absolute left-4 text-gray-400 group-focus-within:text-amber-500 transition-colors">
                                                <i class="fas fa-check-double"></i>
                                            </div>
                                            <input type=@(_showConfirm ? "text" : "password") @bind="_pwd.ConfirmPassword"
                                                   class="w-full pl-11 pr-12 py-3.5 bg-gray-50 rounded-2xl border-2 border-transparent focus:bg-white focus:border-amber-500 focus:ring-4 focus:ring-amber-500/10 outline-none transition-all text-gray-900 font-bold placeholder-gray-400" placeholder="••••••••" />
                                            <button type="button" @onclick="() => _showConfirm = !_showConfirm" class="absolute right-4 text-gray-400 hover:text-gray-700 transition-colors w-6 h-6 flex items-center justify-center">
                                                <i class="fas @(_showConfirm ? "fa-eye-slash" : "fa-eye")"></i>
                                            </button>
                                        </div>
                                    </div>
                                </div>

                                @if (!string.IsNullOrEmpty(_pwdError))
                                {
                                    <div class="p-4 rounded-xl bg-red-50 border border-red-100 flex items-start gap-3 text-red-600">
                                        <i class="fas fa-exclamation-circle mt-0.5"></i>
                                        <p class="text-sm font-bold">@_pwdError</p>
                                    </div>
                                }

                                <div class="pt-4 flex justify-end">
                                    <button @onclick="ChangePasswordAsync" disabled="@_changingPwd"
                                            class="w-full sm:w-auto px-8 py-4 bg-gray-900 text-white font-extrabold rounded-2xl hover:bg-black transition-all shadow-xl shadow-gray-900/20 hover:-translate-y-1 hover:shadow-gray-900/40 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:translate-y-0 relative overflow-hidden group/btn">
                                        <div class="absolute inset-0 w-full h-full bg-gradient-to-r from-transparent via-white/10 to-transparent -translate-x-full group-hover/btn:translate-x-full transition-transform duration-1000 ease-in-out"></div>
                                        <div class="relative flex items-center justify-center gap-2">
                                            @if (_changingPwd)
                                            {
                                                <i class="fas fa-circle-notch animate-spin"></i>
                                                <span>Updating...</span>
                                            }
                                            else
                                            {
                                                <i class="fas fa-lock text-amber-400"></i>
                                                <span>Update Password</span>
                                            }
                                        </div>
                                    </button>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
            }
        </div>
    </Authorized>
</AuthorizeView>



